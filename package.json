{
  "name": "hello-world-lambda-api",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "echo \"Build step for lambda (if needed)\""
  },
  "dependencies": {
    "aws-serverless-express": "^3.4.0",
    "express": "^4.18.2"
  }
}
